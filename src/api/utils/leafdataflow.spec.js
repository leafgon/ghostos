import { chronosDataflow } from "./leafdataflow";


describe("leaf dataflow: util dataflow functions test", () => {

    test("testing chronosDataflow()", async () => {
        const delay$ = new ReplaySubject(1);

        chronosDataflow

    })
})
